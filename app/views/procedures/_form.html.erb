<%= form_for @procedure do |p| %>
  <%= p.label :name %>
  <%= p.text_field :name %>
  <%= p.label :description %>
  <%= p.text_field :description %>
  <%= p.submit "Submit", class:'align-center'%>
<%end%>
